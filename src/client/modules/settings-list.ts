import { fov_current } from "shared/settings/fov_current";
import { mv_forwardspeed } from "shared/settings/mv_forwardspeed";
import { mv_sidespeed } from "shared/settings/mv_sidespeed";
import { sv_password } from "client/settings/sv_password";

const settingsList = {
	fov_current,
	mv_forwardspeed,
	mv_sidespeed,
	sv_password,
};

export default settingsList;
